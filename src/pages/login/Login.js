import React from 'react';
import FormLogin from '../../components/form/FormLogin';

const PagesCadastro = () => {
  return (
    <div>
      <FormLogin />
    </div>
  );
};

export default PagesCadastro;